name: REX Continuous Integration

on:
  push:
    branches: [ develop ]
  pull_request:
    branches: [ develop ]
  release:
    types:
      - created
jobs:
  build:

    runs-on: [ubuntu-20.04]

    strategy:
      matrix:
        node-version: [15.x]

    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v2
      with:
        node-version: ${{ matrix.node-version }}
    - run: npm ci
    - run: npm run affected:lint --base=develop --head=HEAD
    - run: npm run affected:test --base=develop --head=HEAD
    - run: npm run affected:build --base=develop --head=HEAD
    - run: npm run affected:e2e --base=develop --head=HEAD
      env:
        CI: true