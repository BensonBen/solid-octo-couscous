name: REX Continuous Integration

on:
  push:
    branches: [ develop ]
  pull_request:
    branches: [ develop ]
  release:
    types:
      - created
jobs:
  ci-develop:

    runs-on: [ubuntu-20.04]

    strategy:
      matrix:
        node-version: [15.x]

    steps:
    - uses: actions/checkout@v2   
    - uses: actions/setup-node@v2
      with:
        node-version: ${{ matrix.node-version }}
    - run: git fetch origin develop
    - run: sudo npm install -g nx@12.0.8
    - run: npm i --no-save
    - run: nx affected --target=lint --base=develop
    - run: nx affected --target=test --base=develop
    - run: nx affected --target=build --base=develop
    - run: nx affected --target=e2e --base=develop
      env:
        CI: true